This directory contains a support library built by CMake. The support library is statically linked and a version is built for each platform. CMake is more suited for the complex x-platform linking of dependencies than the x-platform scilab compile toolchain.
